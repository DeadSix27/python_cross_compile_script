diff -Nur gnutls-3.6.5.orig/configure gnutls-3.6.5/configure
--- gnutls-3.6.5.orig/configure	2018-12-02 21:24:28.515977718 +0000
+++ gnutls-3.6.5/configure	2018-12-02 21:25:22.555485425 +0000
@@ -10048,12 +10048,12 @@
     pkg_cv_NETTLE_CFLAGS="$NETTLE_CFLAGS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"nettle >= 3.4.1\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "nettle >= 3.4.1") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"nettle >= 3.4\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "nettle >= 3.4") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_NETTLE_CFLAGS=`$PKG_CONFIG --cflags "nettle >= 3.4.1" 2>/dev/null`
+  pkg_cv_NETTLE_CFLAGS=`$PKG_CONFIG --cflags "nettle >= 3.4" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -10065,12 +10065,12 @@
     pkg_cv_NETTLE_LIBS="$NETTLE_LIBS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"nettle >= 3.4.1\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "nettle >= 3.4.1") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"nettle >= 3.4\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "nettle >= 3.4") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_NETTLE_LIBS=`$PKG_CONFIG --libs "nettle >= 3.4.1" 2>/dev/null`
+  pkg_cv_NETTLE_LIBS=`$PKG_CONFIG --libs "nettle >= 3.4" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -10091,9 +10091,9 @@
         _pkg_short_errors_supported=no
 fi
         if test $_pkg_short_errors_supported = yes; then
-	        NETTLE_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "nettle >= 3.4.1" 2>&1`
+	        NETTLE_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "nettle >= 3.4" 2>&1`
         else
-	        NETTLE_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "nettle >= 3.4.1" 2>&1`
+	        NETTLE_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "nettle >= 3.4" 2>&1`
         fi
 	# Put the nasty error message in config.log where it belongs
 	echo "$NETTLE_PKG_ERRORS" >&5
@@ -10129,12 +10129,12 @@
     pkg_cv_HOGWEED_CFLAGS="$HOGWEED_CFLAGS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"hogweed >= 3.4.1\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "hogweed >= 3.4.1") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"hogweed >= 3.4\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "hogweed >= 3.4") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_HOGWEED_CFLAGS=`$PKG_CONFIG --cflags "hogweed >= 3.4.1" 2>/dev/null`
+  pkg_cv_HOGWEED_CFLAGS=`$PKG_CONFIG --cflags "hogweed >= 3.4" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -10146,12 +10146,12 @@
     pkg_cv_HOGWEED_LIBS="$HOGWEED_LIBS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"hogweed >= 3.4.1\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "hogweed >= 3.4.1") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"hogweed >= 3.4\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "hogweed >= 3.4") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_HOGWEED_LIBS=`$PKG_CONFIG --libs "hogweed >= 3.4.1" 2>/dev/null`
+  pkg_cv_HOGWEED_LIBS=`$PKG_CONFIG --libs "hogweed >= 3.4" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -10172,9 +10172,9 @@
         _pkg_short_errors_supported=no
 fi
         if test $_pkg_short_errors_supported = yes; then
-	        HOGWEED_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "hogweed >= 3.4.1" 2>&1`
+	        HOGWEED_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "hogweed >= 3.4" 2>&1`
         else
-	        HOGWEED_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "hogweed >= 3.4.1" 2>&1`
+	        HOGWEED_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "hogweed >= 3.4" 2>&1`
         fi
 	# Put the nasty error message in config.log where it belongs
 	echo "$HOGWEED_PKG_ERRORS" >&5
