diff -Nur filezilla_svn/configure.ac filezilla_svn.patched/configure.ac
--- filezilla_svn/configure.ac	2017-04-21 13:20:57.142228039 +0200
+++ filezilla_svn.patched/configure.ac	2017-04-21 13:20:35.034848774 +0200
@@ -121,15 +121,15 @@
 fi
 
 OLD_DIR="`pwd`"
-mkdir -p "$OLD_DIR/src/fzshellext/32"
+#mkdir -p "$OLD_DIR/src/fzshellext/32"
 mkdir -p "$OLD_DIR/src/fzshellext/64"
 
 AC_ARG_ENABLE(shellext, [], \
         [shellext="$enableval"], [shellext="yes"])
 
 if test "$localesonly" != "yes" && echo $host_os | grep "cygwin\|mingw" > /dev/null 2>&1 && test "$shellext" = "yes"; then
-  cd "$OLD_DIR/src/fzshellext/32" || exit 1
-  "$ac_abs_confdir/src/fzshellext/configure" --prefix="$prefix" --exec-prefix="$exec_prefix" --host=i686-w64-mingw32 || exit 1
+  #cd "$OLD_DIR/src/fzshellext/32" || exit 1
+  #"$ac_abs_confdir/src/fzshellext/configure" --prefix="$prefix" --exec-prefix="$exec_prefix" --host=i686-w64-mingw32 || exit 1
   cd "$OLD_DIR/src/fzshellext/64" || exit 1
   "$ac_abs_confdir/src/fzshellext/configure" --prefix="$prefix" --exec-prefix="$exec_prefix" --host=x86_64-w64-mingw32 || exit 1
 else
diff -Nur filezilla_svn/src/Makefile.am filezilla_svn.patched/src/Makefile.am
--- filezilla_svn/src/Makefile.am	2017-04-21 13:20:45.548788856 +0200
+++ filezilla_svn.patched/src/Makefile.am	2017-04-21 13:20:35.034848774 +0200
@@ -1,5 +1,5 @@
 if MINGW
-  MAYBE_FZSHELLEXT = fzshellext/32 fzshellext/64
+  MAYBE_FZSHELLEXT = fzshellext/64
 endif
 
 if HAVE_LIBPUGIXML
diff -Nur filezilla_svn/src/Makefile.in filezilla_svn.patched/src/Makefile.in
--- filezilla_svn/src/Makefile.in	2017-04-21 13:20:43.586255698 +0200
+++ filezilla_svn.patched/src/Makefile.in	2017-04-21 13:20:35.818877461 +0200
@@ -376,7 +376,7 @@
 top_srcdir = @top_srcdir@
 xdgopen = @xdgopen@
 xgettext = @xgettext@
-@MINGW_TRUE@MAYBE_FZSHELLEXT = fzshellext/32 fzshellext/64
+@MINGW_TRUE@MAYBE_FZSHELLEXT = fzshellext/64
 @HAVE_LIBPUGIXML_FALSE@MAYBE_PUGIXML = pugixml
 @HAVE_DBUS_TRUE@MAYBE_DBUS = dbus
 SUBDIRS = include engine $(MAYBE_PUGIXML) $(MAYBE_DBUS) interface putty $(MAYBE_FZSHELLEXT) .
@@ -699,7 +699,6 @@
 
 
 dist-hook:
-	mkdir -p $(distdir)/fzshellext/32
 	mv -f $(distdir)/fzshellext/64/* $(distdir)/fzshellext
 	rm -rf $(distdir)/config
 	rm -rf $(distdir)/m4
